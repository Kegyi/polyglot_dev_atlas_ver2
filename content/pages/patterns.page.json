{
  "page": "patterns",
  "title": "Patterns",
  "groups": {
    "creational": {
      "title": "Creational Patterns"
    },
    "structural": {
      "title": "Structural Patterns"
    },
    "behavioral": {
      "title": "Behavioral Patterns"
    }
  },
  "content": {
    "singleton": {
      "group": "creational",
      "title": "Singleton Pattern",
      "description": "Ensure a class has only one instance and provide a global point of access to it.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#singleton" }
    },
    "factory_method": {
      "group": "creational",
      "title": "Factory Method Pattern",
      "description": "Define an interface for creating an object, but let subclasses decide which class to instantiate.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#factory_method" }
    },
    "adapter": {
      "group": "structural",
      "title": "Adapter Pattern",
      "description": "Convert the interface of a class into another interface clients expect.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#adapter" }
    },
    "observer": {
      "group": "behavioral",
      "title": "Observer Pattern",
      "description": "Define a one-to-many dependency between objects so that when one object changes state, all its dependents are notified and updated automatically.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#observer" }
    },
    "abstract_factory": {
      "group": "creational",
      "title": "Abstract Factory",
      "description": "Provide an interface for creating families of related or dependent objects without specifying their concrete classes.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#abstract_factory" }
    },
    "builder": {
      "group": "creational",
      "title": "Builder",
      "description": "Separate the construction of a complex object from its representation so the same construction process can create different representations.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#builder" }
    },
    "prototype": {
      "group": "creational",
      "title": "Prototype",
      "description": "Specify the kinds of objects to create using a prototypical instance, and create new objects by copying this prototype.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#prototype" }
    },
    "bridge": {
      "group": "structural",
      "title": "Bridge",
      "description": "Decouple an abstraction from its implementation so the two can vary independently.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#bridge" }
    },
    "composite": {
      "group": "structural",
      "title": "Composite",
      "description": "Compose objects into tree structures to represent part-whole hierarchies so clients treat individual objects and compositions uniformly.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#composite" }
    },
    "decorator": {
      "group": "structural",
      "title": "Decorator",
      "description": "Attach additional responsibilities to an object dynamically without affecting other objects.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#decorator" }
    },
    "facade": {
      "group": "structural",
      "title": "Facade",
      "description": "Provide a unified, higher-level interface that makes a subsystem easier to use.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#facade" }
    },
    "flyweight": {
      "group": "structural",
      "title": "Flyweight",
      "description": "Use sharing to support large numbers of fine-grained objects efficiently.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#flyweight" }
    },
    "proxy": {
      "group": "structural",
      "title": "Proxy",
      "description": "Provide a surrogate or placeholder for another object to control access to it.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#proxy" }
    },
    "chain_of_responsibility": {
      "group": "behavioral",
      "title": "Chain Of Responsibility",
      "description": "Avoid coupling the sender of a request to its receiver by giving more than one object a chance to handle the request.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#chain_of_responsibility" }
    },
    "command": {
      "group": "behavioral",
      "title": "Command",
      "description": "Encapsulate a request as an object, thereby letting you parameterize clients with queues, requests, and operations.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#command" }
    },
    "interpreter": {
      "group": "behavioral",
      "title": "Interpreter",
      "description": "Given a language, define a representation for its grammar along with an interpreter that uses the representation to interpret sentences in the language.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#interpreter" }
    },
    "iterator": {
      "group": "behavioral",
      "title": "Iterator",
      "description": "Provide a way to access the elements of an aggregate object sequentially without exposing its underlying representation.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#iterator" }
    },
    "mediator": {
      "group": "behavioral",
      "title": "Mediator",
      "description": "Define an object that encapsulates how a set of objects interact, promoting loose coupling between them.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#mediator" }
    },
    "memento": {
      "group": "behavioral",
      "title": "Memento",
      "description": "Without violating encapsulation, capture and externalize an object's internal state so it can be restored later.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#memento" }
    },
    "state": {
      "group": "behavioral",
      "title": "State",
      "description": "Allow an object to alter its behavior when its internal state changes.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#state" }
    },
    "strategy": {
      "group": "behavioral",
      "title": "Strategy",
      "description": "Define a family of algorithms, encapsulate each one, and make them interchangeable.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#strategy" }
    },
    "template_method": {
      "group": "behavioral",
      "title": "Template Method",
      "description": "Define the skeleton of an algorithm in an operation, deferring some steps to subclasses.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#template_method" }
    },
    "visitor": {
      "group": "behavioral",
      "title": "Visitor",
      "description": "Represent an operation to be performed on the elements of an object structure without changing the classes of the elements.",
      "code": { "$ref": "/content/codes/json/design_pattern.json#visitor" }
    }
  }
}
